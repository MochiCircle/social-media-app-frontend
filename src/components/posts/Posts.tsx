import React from 'react';

interface IProps {
    postId:number,
    postText:string,
    likes:number
}

export const Post:React.FC<IProps> = (props:IProps) => {
    return (
        <div>

        </div>
    )
}